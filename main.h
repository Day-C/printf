#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>

#include <stdlib.h>
#include <stdio.h>
#include <limits.h>

int str_len(char *s);
int _putchar(char c);
void prt_str(char *str);
void prt_int(int _n);
int _printf(const char *format, ...);

#endif
